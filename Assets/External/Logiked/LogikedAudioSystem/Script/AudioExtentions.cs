using logiked.audio;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class AudioExtentions 
{

    /*
    public static void PlayGameSound(this AudioSource source, GameSound sound)
    {
      
        source.volume = sound.Volume + Random.Range(-sound.Volume_variation, sound.Volume_variation) ;
        source.pitch = sound.Pitch + Random.Range(-sound.Pitch_variation, sound.Pitch_variation);
        source.PlayOneShot(sound.GetAClip);

    }
    */

}
